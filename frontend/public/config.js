// Replaced at container start-up so one web image serves any environment.
// Left empty in dev and in the Android build, where the app falls back to the
// values Vite compiled in from .env.
window.__WHOYAGOT__ = {}
